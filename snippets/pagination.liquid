{% if paginate.pages > 1 %}
<div class="theme-default-pagination">
  <nav class="pagination">
    <ul class="pagination">
      {% if paginate.previous %}
      <li>
        <a href="{{ paginate.previous.url }}" aria-label="Previous" class="prev page-number">
          <span aria-hidden="true">Prev</span>
        </a>
      </li>
      {% else %}
      <li class="disabled prev">
        <a href="#" class="prev page-number">
          <span>Prev</span>
        </a>
      </li>
      {% endif %}
      {% for part in paginate.parts %}
      {% if part.is_link %}
      <li>
        <a  class="page-number" href="{{ part.url }}" title="">{{ part.title }}</a>
      </li>
      {% else %}
      {% if part.title == paginate.current_page %}
      <li class="active"><a  class="page-number" href="">{{ part.title }}</a></li>
      {% else %}
      <li><a  class="page-number" href="#">{{ part.title }}</a></li>
      {% endif %}
      {% endif %}
      {% endfor %}
      {% if paginate.next %}
      <li><a  class="next page-number" href="{{ paginate.next.url }}" title="{{ paginate.next.title }}"><span aria-hidden="true">Next</span></a></li>
      {% else %}
      <li class="disabled next">
        <a href="#" class="next page-number">
          <span>Next</span>
        </a>
      </li>
      {% endif %}
    </ul>
  </nav>
</div>
{% endif %}

<script>
  $(".theme-default-pagination .disabled a"). removeAttr("href");
  $(".theme-default-pagination li.active a"). removeAttr("href");
</script>